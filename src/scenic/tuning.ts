// Visual-only tuning for the scenic header (no business logic)
export const TOPBAR = {
    HEIGHT: 220,       // full hero height
    RADIUS: 14,
    PADDING: 12,
  };
  
  export const LOGO = {
    SIZE: 92,          // splash logo size (px)
  };
  
  export const SKY = {
    GRADIENT: "linear-gradient(180deg, #eaf4ff 0%, #ffffff 70%)",
  };
  
  // Beam visual params
  export const BEAM = {
    LENGTH: 160,
    WIDTH: 18,
    COLOR: "rgba(255,230,140,0.55)",
    GLOW: "0 0 18px rgba(255,200,60,0.6)",
  };